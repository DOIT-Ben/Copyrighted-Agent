# P23-P25 Plan

## Date

- 2026-04-19

## Scope

- P23 rolling baseline archive and latest-auto-compare support
- P24 `/ops` trend watch, provider checklist, and baseline history visibility
- P25 regression, validation, and documentation closeout

## Constraints

- `config/local.json` does not exist in the current workspace
- no real external provider credential or endpoint is configured locally
- real non-sandbox smoke is therefore blocked by environment readiness, not by code readiness

## Execution Order

1. Finish the service and CLI layer for baseline history, delta aggregation, and rolling archive output.
2. Upgrade `/ops` so operators can see not just the latest baseline, but also recent history and provider-gate details.
3. Align unit and integration contracts with the new status semantics and page modules.
4. Run targeted regression, then full regression.
5. Run the new rolling baseline command against real sample inputs.
6. Write docs, issues, validation, playbook updates, and closeout notes.

## Planned Deliverables

- `app/core/services/ops_status.py`
- `app/tools/metrics_baseline.py`
- `app/web/pages.py`
- updated ops integration and unit contracts
- rolling archive artifacts in `docs/dev/history`
- round docs `docs/dev/64-70`

## Exit Criteria

- `/ops` shows `Trend Watch`, `Baseline History`, and `Provider Checklist`
- rolling baseline command writes timestamped archive files
- latest baseline card exposes signed deltas
- regression passes after the UI and contract updates
- docs capture the blocked real-provider dependency explicitly
