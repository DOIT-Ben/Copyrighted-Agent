# 运维页交付收尾可视化 TODO
## 日期

- 2026-04-21

## 目标

- 把 `/ops` 从“技术状态页”进一步收敛为“可直接判断是否能交付”的运维工作台。
- 让业务收尾状态、下一步动作和产物下载不再埋在命令行和 `docs/dev` 文件夹里。
- 保持现有管理台视觉系统，不推翻之前的浅色工作台方向。

## 颗粒化 TODO

1. 用 `ui-ux-pro-max` 做设计系统复核，确认继续沿用管理台风格而不是重刷品牌视觉。
2. 审查 `/ops` 页面当前信息层级，确认“业务收尾”尚未进入主视图。
3. 为业务收尾能力补只读状态加载函数，避免每次打开页面都重跑收尾流程。
4. 为业务收尾产物补下载 helper。
5. 为 API 补 closeout 产物下载路由。
6. 将 closeout 状态注入 `/ops` 页面数据源。
7. 在 `/ops` 顶部 KPI 中加入“业务收尾”状态。
8. 在 `/ops` 状态卡区域加入“业务收尾”卡片。
9. 在 `/ops` 仪表盘中增加高优先级“业务收尾”主面板。
10. 在主面板中展示：
   - 交付结论
   - 当前里程碑
   - 后续动作数量
   - 产物下载入口
   - 逐项检查表
11. 在“这页应该怎么看”快捷区加入“先看业务收尾”。
12. 为新增面板补样式与响应式规则。
13. 修正 `/ops` KPI 栅格，让五张 KPI 卡在桌面端排版稳定。
14. 补充 closeout 只读状态与下载 helper 单测。
15. 更新 `/ops` 页面契约和下载路由契约。
16. 跑前端相关回归。

## 执行顺序

1. 先补后端只读支撑。
2. 再改 `/ops` 页面结构和样式。
3. 然后补测试。
4. 最后跑回归、落文档。
