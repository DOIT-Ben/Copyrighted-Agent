# P17-P22 Plan

## Date

- 2026-04-19

## Scope

- P17 real provider integration readiness
- P18 runtime backup and restore SOP hardening
- P19 baseline trend comparison
- P20 parser quality reduction for real samples
- P21 admin and ops visibility enhancement
- P22 release-readiness closeout

## Execution Order

1. Add reusable provider readiness and probe capability before touching the UI.
2. Surface latest backup, latest baseline, and provider readiness on `/ops`.
3. Run real-sample analysis to identify the exact sources of `needs_review` and `low_quality`.
4. Fix parser false positives first in legacy `.doc`, then in `.pdf`.
5. Re-run baseline comparison against the original snapshot.
6. Run sandbox probe and full regression.
7. Write final docs, issues, validation, and reuse guidance.

## Planned Deliverables

- `app/core/services/provider_probe.py`
- `app/tools/provider_probe.py`
- `/ops` runtime status cards and richer command guidance
- `app/core/services/ops_status.py`
- parser hardening for control-character-heavy legacy `.doc`
- parser hardening for PDF `ToUnicode` stream extraction without new runtime dependencies
- updated config template and runbook guidance
- final comparison artifacts in `docs/dev/56-57`

## Exit Criteria

- sandbox probe command can return structured success
- `/ops` shows latest backup, latest baseline, and provider readiness
- real sample mode A and mode B `needs_review / low_quality` drop to target range
- full regression passes
- docs for plan, build log, issues, validation, playbook, and closeout are complete
