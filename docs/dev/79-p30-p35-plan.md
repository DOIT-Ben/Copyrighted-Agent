# P30-P35 Plan

## Date

- 2026-04-20

## Scope

- P30 local-config bootstrap for repeatable startup
- P31 final provider-probe / release-gate command validation
- P32 `/ops` and page-renderer stabilization
- P33 startup-self-check contract alignment with the new default config presence
- P34 final regression closeout
- P35 documentation and reusable recovery notes

## Constraints

- Real external-provider credentials are still not available locally
- Only desensitized synthetic probe payloads may cross non-mock provider boundaries
- The user wants all artifacts, problems, and lessons persisted under `docs/dev`
- The frontend must stay in a management-system / admin-console form, not a marketing-style page

## Execution Order

1. Compile-check the new provider and ops slice.
2. Run targeted regression and repair any failing contracts.
3. Add a safe default `config/local.json` and validate `provider_probe` / `release_gate`.
4. Run final regression.
5. If unexpected regressions appear, repair them before documentation closeout.
6. Write plan, build log, issues, validation, playbook, and closeout artifacts.

## Deliverables

- `config/local.json`
- `app/web/pages.py`
- updated startup self-check contracts
- new round docs `docs/dev/78-84`
- shared docs updates in `docs/dev/02,04,05,06,07,09`

## Exit Criteria

- the workspace boots with a repeatable mock-safe local config
- `provider_probe` and `release_gate` produce stable local output
- `/ops`, submission pages, case pages, report pages, and browser E2E remain green
- final regression returns to all-pass state
- the encoding-regression recovery path is documented for reuse
