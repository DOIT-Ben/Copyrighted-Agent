# P26-P29 Plan

## Date

- 2026-04-20

## Scope

- P26 provider readiness audit and phase-based status model
- P27 persisted latest-probe artifact and clearer probe remediation
- P28 `/ops` readiness / latest-probe observability
- P29 validation, regression, and documentation closeout

## Constraints

- Local `config/local.json` does not exist as of 2026-04-20
- No real external provider endpoint / model / credential mapping is configured locally
- Only desensitized payload may cross a non-mock provider boundary
- Provider probe validation must use synthetic `llm_safe` payload only
- `app/web/pages.py` already contains a preserved legacy ops renderer backup and should not be cleaned up casually

## Execution Order

1. Audit config, readiness, startup checks, and the current `/ops` provider sections.
2. Introduce a phase-based readiness model with explicit blocking items and remediation text.
3. Persist the latest provider probe result so ops users can inspect it after the CLI command ends.
4. Upgrade startup checks and `/ops` to surface the new readiness and probe status model.
5. Align unit and integration contracts with the new provider observability behavior.
6. Run targeted regression, live sandbox-first probe validation, rolling baseline refresh, and final full regression.
7. Write shared docs updates plus round-specific plan / build / issues / validation / playbook / closeout files.

## Planned Deliverables

- `app/core/services/provider_probe.py`
- `app/tools/provider_probe.py`
- `app/core/services/startup_checks.py`
- `app/web/pages.py`
- `app/web/static/styles.css`
- updated tests for provider probe, startup checks, and `/ops`
- persisted latest probe artifact in `data/runtime/ops`
- round docs `docs/dev/72-77`

## Exit Criteria

- provider readiness distinguishes mock, incomplete, ready, blocked, and probe-result states
- latest probe result is persisted and visible from `/ops`
- sandbox-first probe succeeds without sending raw user material
- rolling baseline history remains stable after the observability changes
- final regression passes
- docs clearly separate environment blockers from code blockers
