# P36-P40 Plan

## Date

- 2026-04-20

## Scope

- P36 inspect stable page-layer boundaries
- P37 extract shared rendering helpers
- P38 extract page-scoped renderers
- P39 keep API imports stable through an export barrel
- P40 validate and document the new structure

## Constraints

- No route behavior regression is acceptable
- Browser E2E and HTML operator flows must stay green
- Existing admin-console visual direction should be preserved
- Real external-provider work remains environment-blocked, so this round focuses on maintainability and safer future UI iteration

## Execution Order

1. Audit current page responsibilities inside `app/web/pages.py`.
2. Create a shared helper module for layout and HTML primitives.
3. Extract renderers by page boundary.
4. Keep `app.api.main` unchanged by preserving the `app.web.pages` import surface.
5. Run compile validation.
6. Run targeted regression, then final full regression.
7. Write docs and reusable lessons.

## Exit Criteria

- the page layer is split into meaningful modules
- FastAPI routes require no import changes
- UI contract tests and browser E2E stay green
- docs capture both the structure map and why the split happened now
