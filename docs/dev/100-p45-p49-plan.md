# P45-P49 Plan

## Date

- 2026-04-20

## Scope

- P45 add a one-command real-provider validation workflow
- P46 cover the new workflow with automated tests
- P47 run the workflow against the current workspace state
- P48 record exact pass criteria and current blockers
- P49 update the runbook and reusable acceptance checklist

## Constraints

- No raw user material may cross the non-mock boundary outside the existing desensitized path
- The final validation flow should be low-cost by default
- The current workspace may still be mock-only, so the tool must also report blocked state clearly

## Execution Order

1. Build a service that chains provider probe, release gate, and sample smoke checks.
2. Add a CLI that writes durable markdown and JSON artifacts.
3. Add blocked-state and sandbox-success coverage.
4. Run the new command against current local config.
5. Update shared logs plus a dedicated acceptance checklist.

## Exit Criteria

- one command exists for real-provider validation
- automated tests prove blocked and pass scenarios
- current workspace state is captured as a durable validation artifact
- pass criteria are explicit for the next real run
