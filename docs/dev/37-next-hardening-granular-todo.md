# 下一阶段细颗粒 Todo

## 日期

- 2026-04-19

## P5 解析质量继续收敛

- [ ] 单独拆解 `2502` 的 `unknown` 材料，记录文件名、parse quality、unknown reason、人工判断结果。
- [ ] 为老 `.doc` 样本建立最小回归语料目录，按“可用文本 / 二进制噪音 / 半可读碎片”分组。
- [ ] 为 `DocBinaryParser` 增加更明确的“文本可用性”判定阈值。
- [ ] 在 UI 中把 `low_quality` 与 `unknown_reason` 展示得更明显，减少人工猜测成本。
- [ ] 为真实样本统计增加“unknown 总数”和“needs_review 总数”的趋势对比。

## P6 浏览器侧体验增强

- [ ] 为模式 B 设计更清晰的批量导入说明与失败回退提示。
- [ ] 增加导入完成后的摘要卡片：材料数、case 数、待复核数、脱敏命中数。
- [ ] 增加更友好的 operator 表单预填和提示文本，减少手工输入出错。
- [ ] 评估是否加入单独的 `Support / Ops` 页面，集中展示配置、日志下载、验证命令。

## P7 真实 AI Provider 接入准备

- [ ] 在 `safe_stub` 之后增加真实 provider adapter 的接口骨架。
- [ ] 约束真实 provider 只能接收 `is_ai_safe_case_payload == True` 的 payload。
- [ ] 为 provider 调用补充超时、失败回退、结构化日志记录。
- [ ] 在 UI 或报告中明确区分“规则结论”和“AI 补充说明”。

## P8 运维与可追溯性

- [ ] 给 `config` 增加示例文件或模板文档。
- [ ] 明确 `data/runtime/` 的清理策略与保留策略。
- [ ] 把常见故障排查步骤写成支持手册，覆盖上传失败、下载失败、SQLite 恢复、日志查看。
- [ ] 增加启动自检，至少覆盖运行目录、数据库文件、日志目录是否可写。

## P9 测试与验收

- [ ] 增加浏览器级 E2E 脚本，覆盖上传、纠偏、下载、日志查看主链路。
- [ ] 把真实样本验证结果沉淀为固定模板，减少每次手工整理成本。
- [ ] 为每个阶段固定“自动化回归 + 真实样本烟测 + 文档落盘”的交付节奏。
