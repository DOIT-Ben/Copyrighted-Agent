# P0 解析质量与 Unknown 收敛计划

## 日期

- 2026-04-19

## 目标

- 修复真实样本 ZIP 中文文件名乱码导致的分类失败。
- 为解析结果补充质量元数据，明确哪些文本可以继续自动分类，哪些必须进入人工复核。
- 把 `unknown` 从“黑盒失败”变成“可解释失败”。
- 在管理台中显式展示待复核材料，给后续 P1 人工纠错打基础。

## 本阶段范围

- ZIP 成员文件名解码修复
- `.doc` / 低质量文本解析质量判定
- 分类调试字段与 unknown 原因
- Submission 详情页待复核队列
- P0 回归测试与真实样本验证

## 不在本阶段处理

- 完整的 `.doc` 高质量解析器替换
- 人工纠错接口与数据库持久化
- 浏览器端模式 B 新交互

## 进入条件

- 隐私链路与输入链路已经稳定
- 当前全量测试已通过
- 已确认真实样本里主要问题集中在 `2502` 与 `2505`

## Done 标准

- `2502` 和 `2505` 的 unknown 明显下降或原因可解释
- 低质量二进制文本不会因为噪声内容误判为可自动归类
- 每个材料都具备 parse quality / triage 元数据
- 页面上能看到待复核队列
- 全量测试通过
- 真实样本验证结果写入 `docs/dev`
