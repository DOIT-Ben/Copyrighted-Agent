# P41-P44 Plan

## Date

- 2026-04-20

## Scope

- P41 audit current local provider readiness and unblock state
- P42 add a contributor map for the modular `app/web` page layer
- P43 add source-safety contracts for export stability and Windows mojibake guardrails
- P44 validate and document the new developer-experience protections

## Constraints

- Do not regress any current route behavior or admin-console rendering
- Real external-provider smoke should only run when local config and credentials actually exist
- The guardrail work should improve future UI iteration safety without forcing route or API changes

## Execution Order

1. Inspect `config/local.json` and local `SOFT_REVIEW_*` variables.
2. If real provider is still blocked, shift to the next non-blocked slice.
3. Add `app/web/README.md` with a module map and safe edit workflow.
4. Add unit contracts around the export barrel and known mojibake markers.
5. Run targeted regression, then final full regression.
6. Capture the result in shared logs plus a round-specific plan, build, issue, validation, playbook, and closeout set.

## Exit Criteria

- the current provider state is explicitly documented
- `app/web` has a durable contributor map
- active web source is protected by automated guardrails
- targeted and full regression remain green
